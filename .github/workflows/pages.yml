# GitHub Pages is configured as: branch main, folder /docs.
# Do NOT force-push gh-pages here — it races the built-in
# pages-build-deployment and leaves the live site stuck on an old build.
name: Pages source check

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Reminder
        run: echo "Site publishes from main/docs via GitHub Pages (legacy)."
